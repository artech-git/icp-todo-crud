// pub trait TodoInterface {
//     fn generate_new_todo(todo: TodoText) -> UID;
//     fn read_todo(&self, uid: UID) -> TodoText;
//     fn return_todo_paginated(&self, start: usize, end: usize) -> Vec<
// }
